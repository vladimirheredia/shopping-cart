import React, {Component} from 'react';
import Nav from '../Nav';
const DogFood = ()=> {
    return(
        <div>
            <Nav/>
            <span>Some Dog Food here...</span> 
        </div>
    );
}

export default DogFood;